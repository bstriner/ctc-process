from setuptools import find_packages, setup

setup(name='asr-vae',
      version='0.0.1',
      install_requires=[],
      author='Ben Striner',
      url='https://github.com/bstriner/asr-vae',
      packages=find_packages())
